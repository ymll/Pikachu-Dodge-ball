using UnityEngine;
using System.Collections;

public class DetectInCourt : MonoBehaviour {
	float tempX, tempZ;
	// Use this for initialization
	void Start () {
		
	}
	
	// Update is called once per frame
	void Update () {
		/*
		tempX = this.transform.position.x;
		tempZ = this.transform.position.z;

		if (GameInfo.playerList [int.Parse (this.name)-1].inGame) {
			//Debug.Log ("Player"+this.name+"is in game now!");
		}else{	//if not in game
			// check if the player wanna step in the court

			this.transform.position = new Vector3 (tempX,this.transform.position.y,tempZ);
		}
		*/
	}

}
